import { Body, Controller, Delete, Get, Param, ParseIntPipe, Patch, Post, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiOperation, ApiParam, ApiTags } from '@nestjs/swagger'; import { PlacesService } from './places.service'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { CreatePlaceDto } from './dto/create-place.dto'; import { UpdatePlaceDto } from './dto/update-place.dto';
@ApiTags('Places')
@Controller('places') export class PlacesController { constructor(private places: PlacesService) {} @Get() @ApiOperation({ summary: 'List published travel places' }) all() { return this.places.findAll(); } @Get(':slug') @ApiOperation({ summary: 'Get one place by slug' }) @ApiParam({ name: 'slug', example: 'kuang-si-waterfalls' }) one(@Param('slug') slug: string) { return this.places.findOne(slug); } @UseGuards(JwtAuthGuard) @ApiBearerAuth() @Post() @ApiOperation({ summary: 'Create a travel place' }) create(@Body() body: CreatePlaceDto) { return this.places.create(body); } @UseGuards(JwtAuthGuard) @ApiBearerAuth() @Patch(':id') @ApiOperation({ summary: 'Update a travel place' }) update(@Param('id', ParseIntPipe) id: number, @Body() body: UpdatePlaceDto) { return this.places.update(id, body); } @UseGuards(JwtAuthGuard) @ApiBearerAuth() @Delete(':id') @ApiOperation({ summary: 'Delete a travel place' }) remove(@Param('id', ParseIntPipe) id: number) { return this.places.remove(id); } }
