import { Body, Controller, Delete, Get, Param, ParseIntPipe, Patch, Post, UseGuards } from '@nestjs/common'; import { PlacesService } from './places.service'; import { JwtAuthGuard } from '../auth/jwt-auth.guard';
@Controller('places') export class PlacesController { constructor(private places: PlacesService) {} @Get() all() { return this.places.findAll(); } @Get(':slug') one(@Param('slug') slug: string) { return this.places.findOne(slug); } @UseGuards(JwtAuthGuard) @Post() create(@Body() body: any) { return this.places.create(body); } @UseGuards(JwtAuthGuard) @Patch(':id') update(@Param('id', ParseIntPipe) id: number, @Body() body: any) { return this.places.update(id, body); } @UseGuards(JwtAuthGuard) @Delete(':id') remove(@Param('id', ParseIntPipe) id: number) { return this.places.remove(id); } }
